using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class #SCRIPTNAME# : MonoBehaviour {

    #region variables

    private StateMachineEngine #SCRIPTNAME#_FSM;

    //Place your variables here

    #endregion variables

    // Start is called before the first frame update
    private void Start()
    {
        #SCRIPTNAME#_FSM = new StateMachineEngine(false);

        CreateStateMachine();
    }

    private void CreateStateMachine()
    {
        // Perceptions
        // Modify or change these for whatever fits your needs
        // You can also create your own Perceptions, see how in the guide
        #PERCEPIONS#
        // States
        #STATES#
        // Transitions#TRANSITIONS#
    }

    // Update is called once per frame
    private void Update()
    {
        #SCRIPTNAME#_FSM.Update();
    }

    // Create your desired actions to be taken in each state
    #ACTIONS#
}