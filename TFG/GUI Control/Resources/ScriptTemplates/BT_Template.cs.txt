using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class #SCRIPTNAME# : MonoBehaviour {

    #region variables

    private BehaviourTreeEngine #SCRIPTNAME#_BT;

    //Place your variables here

    #endregion variables

    // Start is called before the first frame update
    private void Start()
    {
        #SCRIPTNAME#_BT = new BehaviourTreeEngine(false);

        CreateBehaviourTree();
    }

    private void CreateBehaviourTree()
    {
        // Nodes

        #NODES#
        // Child adding
        #CHILDS#
        // SetRoot
        #SETROOT#
    }

    // Update is called once per frame
    private void Update()
    {
        #SCRIPTNAME#_BT.Update();
    }
    
    // Create your desired actions to be taken in each state
    #ACTIONS#
}